all:
  children:
    docker_swarm-servers:
      children:
        ds-masters:
          hosts:
            ds-master01:
              ansible_host: ec2-13-39-242-54.eu-west-3.compute.amazonaws.com

    #     ds-workers:
    #       hosts:
    #         ds-node01:
    #           ansible_host:
    #         ds-node02:
    #           ansible_host:

    # gitlab_runner:
    #   hosts:
    #     gitlab-runner01:
    #       ansible_host: